# 最终测试前检查清单 v1

这份清单用于正式跑 Baseline、消融和完整方案之前做最后确认。

## 已完成的内部准备

| 项目 | 状态 | 证据 |
|---|---|---|
| 核心对比集数量和类别固定 | 已完成 | 30 条，6 类 |
| 每条题有预期最终状态 | 已完成 | `expected_world_state` |
| 成功题有目标/目的地标准答案 | 已完成 | `expected_target_ids`、`expected_destination_id` |
| V0/V1/V2/V4 变体入口 | 已完成 | `run_closed_loop_benchmark.py --variant` |
| 每条结果写入实验编号、协议版本、Git SHA、seed | 已完成 | benchmark report record |
| 目标绑定和世界状态自动判定 | 已完成 | `semantic_exact_match_rate`、`false_success_rate` |
| 跨变体差值汇总 | 已完成 | `compare_experiment_reports.py` |
| 协议和题集预检 | 已完成 | `experiment_protocol_preflight.json` 为 PASS |

## 正式运行前必须确认

1. 本轮实验编号固定为 `exp-20260828-01`，V0/V2/V4 使用同一个编号。
2. 确认当前代码、测试集、模型、策略档位和重试配置不再修改，并重新记录最终 Git SHA。
3. 确认 CodeArts 账号、CLI 和 TraceCoder 配置可用；密钥只放在本机环境，不写入报告。
4. 先跑每个变体的 `--representative --repeats 1`，代表题全部符合预期后，再跑 30 条×5 次。
5. V2 只关闭 D；V4 保留 A/B/C/D。不得通过删题、改预期或临时改 Prompt 修饰结果。
6. Mock、在线 CodeArts、Isaac Sim 分开保存和统计。

## 正式测试顺序

```text
协议预检 → 配置/凭证检查 → V0 代表集 → V2 代表集 → V4 代表集
→ V0/V2/V4 各 30 条×5 次 → 汇总差值 → 留出集泛化 → Isaac Sim/HIL 证据
```

## 当前不能直接宣称的内容

- V0 的全量预检只证明基线链路和判定器可运行，不等于完整方案成绩。
- V2/V4 需要真实 CodeArts 凭证后才能正式运行。
- V3 无安全门禁实验仍未开放，只能在隔离仿真开关完成后进行。
- 当前工作区若还有代码改动，之前生成的报告只能作为预检证据，不能作为最终 PPT 成绩。
