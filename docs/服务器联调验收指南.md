# C 模块 · 服务器联调验收指南

> 分支：`feature/executor-isaac-v2`（已推 GitHub，PR #5）
> 前置：Isaac 后端 + local/sim/real 配置 + stack_on 已集成到新 main，C 单测 37 项全绿。

本文指导在服务器 Isaac Sim / 真机环境下，用**新 executor 链路**完成 C 模块验收。

---

## 一、验收判据（P0/P1）

同一条 `strategy.v1` 在 Mock 和 Isaac Sim 下产出**可对照的 `execution.v1`**：

- `status == SUCCEEDED`
- `cube_before` / `cube_after` 位姿变化（`cube_moved_m > 0`）
- `steps` 含五步原子动作记录，detect 步骤带 `pose` 证据
- `trajectory_points` 含关节轨迹
- `provenance.backend == "isaac"`
- 安全事件 `safety_events` 为空（正常任务）

---

## 二、传输到服务器

```bash
# 本机（Windows，PowerShell）—— 脚本已随仓库在 tools/ 下，无需单独传
scp -P 5122 -r <仓库代码> stu_01@10.16.0.40:/data/stu_01/workspace/

# 服务器（确保容器 UID 可写）
ssh -p 5122 stu_01@10.16.0.40 "sudo chown -R 1234:1234 /data/stu_01/workspace"
```

---

## 三、容器内运行（Isaac Sim 6.0，无头 + CPU）

```bash
docker run --rm \
  --entrypoint bash \
  --gpus '"device=0"' \
  --network none \
  -u 1234:1234 \
  -e ACCEPT_EULA=Y \
  -e PRIVACY_CONSENT=N \
  -e ISAACSIM_ASSET_ROOT=/isaacsim_assets/Assets/Isaac/6.0 \
  -v /data/stu_01/workspace/codearts-executor-isaac-v2:/workspace \
  -v /data/stu_01/isaac_assets:/isaacsim_assets:ro \
  nvcr.io/nvidia/isaac-sim:6.0.0 \
  -lc 'cd /isaac-sim && ./python.sh /workspace/tools/run_executor_acceptance.py --result-dir /workspace/results --/app/headless=true --/persistent/isaac/asset_root/default=/isaacsim_assets/Assets/Isaac/6.0'
```

> 关键：镜像默认 entrypoint 是流式应用，**必须 `--entrypoint bash` 覆盖**再
> `cd /isaac-sim && ./python.sh ...`；`--/app/headless=true` 与资产路径两个参数都要。
> 资产目录名以宿主机实际为准（`/data/stu_01/isaac_assets`）。

可选参数 `--placement-mode direct|stack_on`（默认 direct）。

---

## 四、看结果

```bash
cat /workspace/results/execution.json
cat /workspace/results/progress.jsonl
```

预期 `execution.json` 里：

```json
{
  "schema_version": "execution.v1",
  "status": "SUCCEEDED",
  "provenance": {"backend": "isaac", "agent": "executor", ...},
  "cube_before": [0.25, 0.0, 0.12],
  "cube_after":  [0.4, 0.0, 0.03],
  "cube_moved_m": 0.15,
  "steps": [...五步..., "pose": {...}]
}
```

---

## 五、Mock vs Isaac 对照（本地即可，无需服务器）

同一策略在 Mock 下跑一遍，对比 `steps` 结构、状态、证据字段是否同构：

```bash
python -m unittest tests.unit.test_isaac_backend -v   # 后端语义
```

或走适配器（本地 mock）：

```python
from integration.adapters import perception
from integration.adapters.executor import ExecutorAdapter
from integration.config.loader import load_profile

scene = perception.run({"scene_id": "stacking_cubes", "backend": "mock"})
adapter = ExecutorAdapter.from_profile(load_profile("local"), scene)
out = adapter.run(<同一条 strategy.v1>)
```

---

## 六、已知限制（务必先读）

1. **CPU 物理下 `OmniDriver.move_to` 远距离移动每帧耗时指数增长**（20ms→3.6s→20s→126s/帧）。
   - 验收脚本用小范围方块（约 0.15m 移动），前 ~33 帧稳定 20ms/帧。
   - 若整段 pick-and-place 过慢，改用官方 `FrankaPickPlace` 作为执行引擎
     （`tools/run_isaac_pickplace.py`，实测稳定 453ms/帧、280 帧 done），
     5 动作接口代码保留，后续修好再切回。
2. **CUDA 物理段错误**：全程用 `set_physics_sim_device("cpu")`。
3. **无头截图失败**：验收以「物体位置 + 关节角度」为准，截图不必要。

---

## 七、真机（real profile）注意事项

真机后端 `RealRobotBackend` 默认 `require_human_confirmation=True`：
运动动作前必须 `confirm(operator_id)`，否则返回 `HUMAN_CONFIRMATION_REQUIRED`。
真机限速 0.05 m/s、工作空间 x/y ±0.3、z 0.02–0.45（`integration/config/profiles/real.toml`）。

---

## 八、全链路 A→B→C→D（后续）

C 的 executor 已通过 `from_profile` 接入 `integration/pipeline.py`（`adapters["executor"]`）。
全链路联调需 A/B/D 的 DeepSeek API Key + Isaac Sim 同机，由队长协调；
C 侧无需再改接口，照 `ExecutorAdapter.from_profile(profile, perception, driver=OmniDriver(...))` 装配即可。
