## **第二周核心目标**：实现自然语言到 Isaac Sim 仿真机器人的闭环

### 一、 第一周阶段性成果总结

1. 建立了最前沿的 **Isaac Sim 6.0.1 + Franka Panda 7-DOF** GPU 物理仿真环境；高质量完成了 `T001-T010` 共 10 个典型任务的结构化 JSON Schema 契约初稿。
2. 深度学习了《Code as Policies》《SayCan》《TraceCoder》《Voyager》等 8 篇 CCF A/B 类顶会方法论，将“排序、条件筛选、形状匹配”等复杂逻辑完美融入赛题要求，确立了系统的三阶段处理架构。

### 二、 团队分工与职责

#### A 王翊航/郭家腾（意图解析与交互）

- **职责定位**：将自然语言和环境基础信息合并为标准无歧义的“结构化需求 JSON”，并搭建演示 UI。

- **短期重点任务**：

  与吴昌庆配合，结合 `T001-T010` 及排序/条件筛选任务，产出最终JSON规范。

  编写并测试能够将自然语言稳定转化为规范 JSON 的 System Prompt。

#### B 冯海（CodeArts 智能体与策略代码）

- **职责定位**：负责配置与对接华为云 CodeArts 智能体，让大模型读取 JSON 并生成带有 `if-else` 和空间计算的 Python 策略代码。

- **短期重点任务**：

   配合C工作，接收机器人元API说明书，并配置 CodeArts 智能体的“系统系统说明书”。

  参照 *Code as Policies (CaP)*，编写 3-5 个 Few-shot 样例提示词，教大模型通过 NumPy 编写调用元 API 的控制脚本。

  构建脚本，对 CodeArts 输出的代码进行基础语法校验与安全过检。

#### C 吴昌庆（物理仿真与环境感知）

- **职责定位**：负责 Isaac Sim 仿真平台维护、环境感知数据输出以及机器人底层运动学（元 API）的物理执行。

- **短期重点任务**：

  维护并熟练使用 Isaac Sim 仿真测试环境，搭设标准测试场景（如方块堆叠、多尺寸杯子排列、条件分类选物）。

  编写感知脚本 ，实时抓取仿真环境中物体的名称、3D坐标和尺寸，打包成 JSON 供给 A 和 B。

  编写机器人元API说明书，接收 B 传来的 Python 代码并挂载到机械臂上执行，暴露对应的元 API 接口。

#### D 王翊航/郭家腾（闭环纠错）

- **职责定位**：负责系统前后端通信打通，并在机械臂执行物理报错时，捕获错误日志并引导大模型自我修正。

- **短期重点任务**：

  参照 *TraceCoder* 思路，在仿真执行脚本中加入探针。一旦触发抓空、碰撞或超限界，立即截获错误码和实时坐标，生成 `error_report.json`。

### 三、本周预期目标与产物

#### **目标**：

**M1：** 确定《需求 JSON规范》、《机器人元 API 说明》、《诊断日志 JSON 规范》的格式和字段接口。

**M2：** 四个模块独立进行测试跑通。

**M3：** 完成MVP单链路贯通测试——在交互界面输入一句自然语言，让 Isaac Sim 虚拟环境里的 Franka Panda 机械臂成功做出一连串正确的物理动作。

#### **产物**：

### A

**重点目标**：打通口语转 JSON，搭建可视化交互网页 

1. 标准结构化需求 JSON 模板规范。
2. 口语转 JSON 的系统提示词。
3. 前端口语转换json交互展示。

### B

**重点目标**：调教 CodeArts，把 JSON 翻译为带几何计算的 Python 控制脚本

1. CodeArts 提示词文件（含元 API 声明 + 4 个 CaP 代码样例）。
2. 校验拦截脚本（检测语法，拦截非法函数调用）。

### C

**重点目标**：维护 Isaac Sim ，输出感知坐标，把底层运动封装成元 API。

1. 元 API 说明书（明确函数的参数与安全红线）。
2. 元 API 底层驱动脚本（控制机械臂，内置防撞物理断言）。
3. 环境感知脚本（实时抓取物体 3D 坐标与尺寸，生成 JSON）。

### D

**重点目标**：打通前后端数据链，植入底层监控探针。 

1. FastAPI/Flask 中转服务（转发 UI 请求、调用 CodeArts、下发仿真指令）。
2. TraceCoder运行态探针脚本（监听碰撞/抓空，抓取现场坐标输出 `error_report.json`）。



后续内容全部上传到github仓库